import React from 'react';
import { SectionTitle } from '../ui/Typography';

export const CommissionTimeline = () => {
  // Can be filled with Commission history timeline items.
  return (
    <div>
       <SectionTitle>Commission Timeline</SectionTitle>
       {/* To be implemented */}
    </div>
  );
};
